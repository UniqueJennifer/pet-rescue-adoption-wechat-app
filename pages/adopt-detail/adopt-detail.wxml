<view class="page">
  <view wx:if="{{pet}}">
    <swiper
      class="detail-swiper"
      indicator-dots="{{images.length > 1}}"
      indicator-color="rgba(255, 253, 246, 0.72)"
      indicator-active-color="#087e72"
      circular="{{images.length > 1}}"
    >
      <swiper-item wx:for="{{images}}" wx:key="*this">
        <image
          class="detail-image"
          src="{{item}}"
          mode="aspectFill"
          data-url="{{item}}"
          bindtap="previewImage"
        ></image>
      </swiper-item>
    </swiper>

    <view class="detail-card">
      <view class="detail-head">
        <view>
          <text class="detail-name">{{pet.name}}</text>
          <text class="detail-meta">{{pet.type}} · {{pet.breed || '未知品种'}} · {{pet.age}} · {{pet.gender}}</text>
        </view>
        <text class="pill">{{pet.status}}</text>
      </view>
      <text class="detail-owner">发布者：{{pet.ownerName || '爱心用户'}}</text>
    </view>

    <view class="detail-card">
      <text class="detail-section-title">基本资料</text>
      <view class="info-row">
        <text class="info-label">类型</text>
        <text class="info-value">{{pet.type || '未填写'}}</text>
      </view>
      <view class="info-row">
        <text class="info-label">品种</text>
        <text class="info-value">{{pet.breed || '未填写'}}</text>
      </view>
      <view class="info-row">
        <text class="info-label">年龄</text>
        <text class="info-value">{{pet.age || '未填写'}}</text>
      </view>
      <view class="info-row">
        <text class="info-label">公母</text>
        <text class="info-value">{{pet.gender || '未填写'}}</text>
      </view>
      <view class="info-row">
        <text class="info-label">来源</text>
        <text class="info-value">{{pet.sourceDisplay}}</text>
      </view>
    </view>

    <view class="detail-card">
      <text class="detail-section-title">健康信息</text>
      <view class="info-row">
        <text class="info-label">健康状况</text>
        <text class="info-value">{{pet.healthStatusDisplay}}</text>
      </view>
      <view class="info-row" wx:if="{{pet.diseaseInfo}}">
        <text class="info-label">疾病信息</text>
        <text class="info-value">{{pet.diseaseInfo}}</text>
      </view>
      <view class="info-row" wx:if="{{pet.injuryInfo}}">
        <text class="info-label">伤残情况</text>
        <text class="info-value injury-value">{{pet.injuryInfo}}</text>
      </view>
      <view class="info-row">
        <text class="info-label">疫苗</text>
        <text class="info-value">{{pet.vaccinated || '未填写'}}</text>
      </view>
    </view>

    <view class="detail-card">
      <text class="detail-section-title">位置信息</text>
      <view class="info-row">
        <text class="info-label">所在位置</text>
        <text class="info-value">{{pet.locationDisplay}}</text>
      </view>
      <view class="info-row" wx:if="{{pet.isBase}}">
        <text class="info-label">基地位置</text>
        <text class="info-value base-location-value">{{pet.baseLocationDisplay || '基地内位置未填写'}}</text>
      </view>
      <view class="info-row" wx:if="{{pet.canWalkDisplay}}">
        <text class="info-label">遛狗预约</text>
        <text class="info-value walk-status-value">{{pet.canWalkDisplay}}</text>
      </view>
    </view>

    <view class="detail-card">
      <text class="detail-section-title">情况简介</text>
      <text class="detail-intro">{{pet.intro}}</text>
    </view>

    <button class="primary-btn contact-detail-btn" bindtap="showContact">联系领养</button>
  </view>
  <view wx:else class="empty">正在加载领养信息</view>
</view>
